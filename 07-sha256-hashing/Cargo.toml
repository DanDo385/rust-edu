[package]
name = "sha256-hashing"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[[bin]]
name = "sha256-hashing"
path = "src/main.rs"

[dependencies]
# Using std library only - implementing SHA-256 and hex encoding manually for education

[dev-dependencies]

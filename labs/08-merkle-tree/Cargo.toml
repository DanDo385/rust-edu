[package]
name = "merkle-tree"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[[bin]]
name = "merkle-tree"
path = "src/main.rs"

[dependencies]
# Using std library only - implementing hashing manually for education

[dev-dependencies]

[package]
name = "simple-blockchain"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[[bin]]
name = "simple-blockchain"
path = "src/main.rs"

[dependencies]
# Using std library only - manual implementations for educational purposes

[dev-dependencies]

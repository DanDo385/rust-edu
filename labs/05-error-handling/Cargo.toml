[package]
name = "error-handling"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[[bin]]
name = "error-handling"
path = "src/main.rs"

[dependencies]
# Using std library only - no external dependencies needed

[dev-dependencies]
